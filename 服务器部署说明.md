# SoulMate AI 服务器部署说明

## 服务器分配

| 服务器 | IP | 角色 | 配置 |
|--------|-----|------|------|
| 服务器 1 | 154.209.5.177 | 数据库 (PostgreSQL + Redis) | 2C2G |
| 服务器 2 | 39.108.137.45 | 应用 (Spring Boot + Nginx) | 2C2G |

## 服务器 1 - 数据库

```bash
# SSH 登录
ssh root@154.209.5.177

# 启动数据库
cd /opt
docker compose -f docker-compose.server1.yml up -d
```

已运行的容器:
- soulmate-postgres (pgvector/pgvector:pg16) - 端口 5432
- redis-stack (redis/redis-stack-server) - 端口 6379

数据库: soulmate / 用户: soulmate

## 服务器 2 - 应用

```bash
# SSH 登录
ssh -i 阿里云.pem root@39.108.137.45

# 克隆代码
cd /opt
git clone https://github.com/eggCodedmb/soulmate-ai-server.git soulmate-ai
cd /opt/soulmate-ai

# 创建 docker-compose.app.yml (参考 docker-compose.server2.yml)

# 下载嵌入模型
mkdir -p /opt/soulmate-ai/models/e5-small-v2
cd /opt/soulmate-ai/models/e5-small-v2
curl -L -o model.onnx https://hf-mirror.com/sentence-transformers/all-MiniLM-L6-v2/resolve/main/onnx/model.onnx
curl -L -o tokenizer.json https://hf-mirror.com/sentence-transformers/all-MiniLM-L6-v2/resolve/main/tokenizer.json

# 构建启动
docker compose -f docker-compose.app.yml up -d --build
```

## 端口放行

### 服务器 1 (154.209.5.177)
- 5432: PostgreSQL - 仅允许服务器 2 访问
- 6379: Redis - 仅允许服务器 2 访问

### 服务器 2 (39.108.137.45)
- 80: Nginx HTTP (支付宝回调必需)
- 443: Nginx HTTPS (域名上线后)

## 本地开发

```bash
# 启动本地基础设施
docker compose -f docker-compose.dev.yml up -d

# IDE 中启动 SoulMateApplication
# 应用默认连接 localhost:5432 和 localhost:6379
```
