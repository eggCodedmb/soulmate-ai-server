spring:
  ai:
    embedding:
      transformer:
        onnx:
          modelUri: classpath:models/e5-small-v2/model.onnx
        tokenizer:
          uri: classpath:models/e5-small-v2/tokenizer.json
    vectorstore:
      pgvector:
        table-name: memory_vectors
        initialize-schema: true

soulmate:
  file:
    base-dir: /app/data
